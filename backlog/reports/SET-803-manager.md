# SET-803 — Persona Manager Report (Andreea Mitran)

**Verdict: BUY**

Audit log settings page with aggregated view (HR + CRM sources), filters by actor/action/date,
CSV export. GDPR page: DPA download (pre-filled with tenant name), data retention config
(auto-delete lost leads after N days, anonymize inactive students).

LOVES: Audit log cu filtre → pot investiga cine a șters ce și când.
CSV export → formatdard pentru auditori externi.
DPA pre-completat → nu mai aleg șabloane, doar imprim și semnez.

USEFUL: Retenție date configurabilă → GDPR compliance fără efort manual.

WOULD LIKE: Email automat când se aplică o ștergere automată.
Business case: GDPR fine can be 4% global revenue. This pays for itself.
